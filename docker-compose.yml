version: '2'
services:
    sts-bundle:
        build:
            context: .
            args:
            - JDK_REPO=ppa:openjdk-r/ppa
            - JDK_VERSION=openjdk-8-jdk
#       ports:
#       - "2222:22"
        container_name: sts-bundle
#        privileged: true
#       extra_hosts:
#       - "docker-virtual.art.local docker-dev-local2.art.local docker-prod-local2.art.local:192.168.99.100"
        environment:
        - DISPLAY=192.168.99.100:0